# 协作约束

## 适用范围

所有协作者、自动化代理和仓库交付物均遵循本文件。需求尚处讨论或未派工阶段时，不得自行实施。

## 可移植性与隐私

- 源码、文档、模板、消息及生成产物不得添加个人署名。
- 不得硬编码开发者个人绝对路径、账号、凭据或机器配置。
- 文件路径从项目根目录、配置项或运行时输入解析。
- 配置示例仅包含非敏感示例；真实凭据、运行数据、日志、缓存、备份不进入 Git。
- 不修改或伪造 Git 作者历史。署名规则针对交付内容，不要求抹除 Git 协作审计记录。

## 执行原则

- 确定性流程优先脚本化。
- Agent 不得绕过权限、固定业务规则及人工审批。
- 外部写入受理不明时先回查，不盲目重发。
- 未获明确授权，不自动提交、推送、改远程仓库设置或打 tag。

## 提交与验证

- 按任务隔离改动；不得夹带真实业务数据或本地备份。
- 本地钩子可被跳过；远程 CI 和受保护分支才是多人合并约束。
- CI 通过只代表仓库检查通过，不代表业务可用。
