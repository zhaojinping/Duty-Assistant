# 贡献与仓库调度

仓库所有者担任主控。邀请协作者不等于任务指派。先在 Issue 中由主控确认负责人和范围，再开分支。

1. 读取本文件与 `AGENTS.md`。不得扩大任务范围。
2. 安装本地钩子：`python scripts/install_hooks.py`。针对任务开独立分支，不直接向 `main` 推送业务改动。
3. 只改任务授权目录。
4. 提交 PR，关联 Issue，提供实际测试命令与结果（进入开发阶段后测试数量不得为零）、失败边界和外部副作用说明。
5. 主控审核后决定是否合并。不要自行关闭需要主控真实验收的卡。

所有公开材料不得含个人署名、个人绝对路径、真实人员/群/表 ID、内部台账、访问链接或密钥。源码中的合成测试标识必须显式标明。Git 作者元数据正常保留。

仓库 guard 是保守检查，不覆盖所有敏感信息形态；提交者必须人工检查 diff。CI 通过不等于业务可用。
